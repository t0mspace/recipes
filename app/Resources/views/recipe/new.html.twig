{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recipe creation</h1>

    {{ form_start(form) }}
    {{ form_row(form.kind_dish) }}
        {#<input type="submit" value="Create" />#}
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('recipe_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
