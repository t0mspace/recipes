{% extends 'base.html.twig' %}

{% block body %}
    <h1>User edit</h1>
    {{ form_start(form) }}
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('user_change_password', {'id': app.user.id}) }}">Back to the list</a>
        </li>
        <li>
        </li>
    </ul>
{% endblock %}
